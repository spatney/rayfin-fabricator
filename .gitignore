node_modules/
out/
dist/
dist-electron/
*.log
.boot.*
.DS_Store
.env
.env.local
.env.*.local
out-tsc/
.vite/
*.tsbuildinfo

# Local workspace where user Rayfin projects are created (kept out of this repo)
workspace/

# Telemetry/Azure endpoints injected at deploy time (per-deployment, never committed)
resources/telemetry.json
.deploy.state.json

# Universal-template dependency artifacts (prebuilt node_modules + warm npm cache).
# Generated per-platform by the release workflow; never committed. The directory
# itself is kept (with a README placeholder) so the Tauri resource path resolves.
resources/fabricator-universal-deps/*.tgz

# Code-signing overlay generated by the release workflow (never committed)
src-tauri/tauri.signing.conf.json
